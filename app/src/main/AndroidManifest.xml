<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shtainyky.tvprogram">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:allowTaskReparenting="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".loading_data.LoadingDataActivity"
            android:screenOrientation="portrait" />

        <service
            android:name=".service.UpdatingTodayProgramIntentService"
            android:exported="false" />
        <service
            android:name=".service.LoadingMonthProgramsIntentService"
            android:exported="false" />

        <provider
            android:name=".database.MyContentProvider"
            android:authorities="com.shtainyky.tvprogram"
            android:exported="false" />

        <service
            android:name=".loading_data.LoadingDataIntentService"
            android:exported="false"></service>
    </application>

</manifest>